# CI reutilizable para productos Next.js + Supabase. Un solo job por etapa para que el ruleset
# de main pueda exigir el check "ci". Objetivo: < 8 min con caché de pnpm y de navegadores.
name: ci-next-supabase

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        default: "22"
      run-e2e:
        type: boolean
        default: true
      coverage-threshold:
        type: number
        default: 70

permissions:
  contents: read
  pull-requests: read

jobs:
  titulo-pr:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          # Uno por línea: la acción parte este input por saltos de línea, así que en una sola
          # línea lo toma como un único tipo llamado "feat fix docs …" y ningún título valida.
          types: |
            feat
            fix
            docs
            style
            refactor
            perf
            test
            build
            ci
            chore
            revert
          requireScope: false
          subjectPattern: ^.{1,80}$

  secretos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with: { fetch-depth: 0 }
      # Imagen oficial en vez de gitleaks-action (que exige licencia para organizaciones).
      - name: gitleaks
        run: |
          docker run --rm -v "$PWD:/repo" ghcr.io/gitleaks/gitleaks:latest \
            git /repo --no-banner --redact --exit-code 1 --report-format sarif --report-path /repo/gitleaks.sarif
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks
          path: gitleaks.sarif
          retention-days: 7

  calidad:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm format:check
      - run: pnpm knip
      - run: pnpm depcruise
      - run: pnpm audit --audit-level=high
      - run: pnpm test -- --coverage.enabled --coverage.thresholds.lines=${{ inputs.coverage-threshold }}

  base-de-datos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with: { fetch-depth: 0 }
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - uses: supabase/setup-cli@v1
        with: { version: latest }
      - name: Linter de migraciones nuevas (squawk)
        if: github.event_name == 'pull_request'
        run: |
          nuevas=$(git diff --name-only --diff-filter=A origin/${{ github.base_ref }}...HEAD -- supabase/migrations/ | grep -E '\.sql$' || true)
          if [ -n "$nuevas" ]; then echo "$nuevas" | xargs pnpm squawk; else echo "sin migraciones nuevas"; fi
      - name: Migraciones editadas (prohibido)
        if: github.event_name == 'pull_request'
        run: |
          editadas=$(git diff --name-only --diff-filter=MD origin/${{ github.base_ref }}...HEAD -- supabase/migrations/ || true)
          if [ -n "$editadas" ]; then echo "Migraciones ya fusionadas modificadas o borradas:"; echo "$editadas"; exit 1; fi
      - run: supabase start -x studio,imgproxy,inbucket,logflare,vector,edge-runtime,realtime
      - run: supabase db reset --local
      - run: supabase test db --local
      - name: Tipos generados sin drift
        run: |
          pnpm db:types
          git diff --exit-code -- lib/supabase/tipos.ts || { echo "Corre pnpm db:types y commitea los tipos"; exit 1; }
      - if: always()
        run: supabase stop --no-backup

  build-y-e2e:
    runs-on: ubuntu-latest
    needs: [calidad]
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ inputs.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - uses: supabase/setup-cli@v1
        if: inputs.run-e2e
        with: { version: latest }
      - if: inputs.run-e2e
        run: supabase start -x studio,imgproxy,inbucket,logflare,vector,edge-runtime,realtime
      - name: Variables para build y e2e
        run: |
          if [ "${{ inputs.run-e2e }}" = "true" ]; then
            # `status -o env` imprime KEY="valor"; se quitan las comillas para que sean URLs válidas.
            supabase status -o env \
              | grep -E '^(API_URL|PUBLISHABLE_KEY)=' \
              | sed -E 's/^API_URL=/NEXT_PUBLIC_SUPABASE_URL=/; s/^PUBLISHABLE_KEY=/NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=/; s/="(.*)"$/=\1/' \
              >> "$GITHUB_ENV"
          else
            echo "NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321" >> "$GITHUB_ENV"
            echo "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=build" >> "$GITHUB_ENV"
          fi
      - run: pnpm build
      - if: inputs.run-e2e
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - if: inputs.run-e2e
        run: pnpm exec playwright install --with-deps chromium
      - if: inputs.run-e2e
        run: pnpm e2e
      - if: inputs.run-e2e && failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 7
      - if: always() && inputs.run-e2e
        run: supabase stop --no-backup

  ci:
    if: always()
    runs-on: ubuntu-latest
    needs: [titulo-pr, secretos, calidad, base-de-datos, build-y-e2e]
    steps:
      - name: Resultado
        run: |
          if echo '${{ toJSON(needs) }}' | jq -e 'to_entries | all(.value.result == "success" or .value.result == "skipped")' >/dev/null; then
            echo "ci en verde"
          else
            echo "ci en rojo"; exit 1
          fi
